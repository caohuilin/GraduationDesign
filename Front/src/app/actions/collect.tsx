import { createAction } from 'redux-actions';

export const collectBook  = createAction('collect book');
export const getCollectBooks  = createAction('get collect books');
export const getRecommendBooks  = createAction('get recommend books');
