import { createAction } from 'redux-actions';

export const getPageWidth = createAction('get page width');
export const saveUserStatus = createAction('save user status');
export const getUserStatus = createAction('get user status');
